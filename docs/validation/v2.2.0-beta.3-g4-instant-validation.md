# v2.2.0-beta.3 G4 Instant physical validation

## Current result

`RESULT: PENDING — PRE-AUDIO GATE ONLY`

No sound has been played on the G4 Instant for this candidate. This document records read-only discovery and automated preparation only. The PR must remain draft until the exact frozen candidate passes the owner-observed physical gate.

## Candidate identity

- Version: `2.2.0-beta.3`
- Candidate SHA: freeze after final pre-deployment review
- Stable rollback target: `v2.1.8`
- Current deployed prerelease rollback target: `v2.2.0-beta.2`
- Representative target: one UVC G4 Instant

No private addresses, hostnames, credentials, device IDs, signed URLs, cookies, or private topology belong in this report.

## Read-only discovery — COMPLETE

- [x] Protect reports the representative G4 Instant connected and recording.
- [x] Protect reports a speaker.
- [x] Bootstrap metadata reports AAC, `serverudp`, 22,050 Hz, mono, 16-bit talkback.
- [x] The profile matches the existing low-level AAC/ADTS implementation dimensions.
- [x] No talkback session was negotiated or prepared.
- [x] No audio frame was sent.
- [x] No camera, Protect, Docker, or Home Assistant setting was changed.

This metadata match is necessary but not sufficient evidence of compatibility.

## Automated candidate gate

- [x] `EXPERIMENTAL_CAMERA_PROFILES` defaults to empty.
- [x] G4 remains unavailable without explicit opt-in.
- [x] Exact five-dimensional AAC profile opt-in reports `experimental_opt_in`.
- [x] Existing G3 profile remains `physically_validated`.
- [x] Profile mismatch fails before talkback preparation.
- [x] Malformed, duplicate, extra-key, unsupported, and type-coerced profile configuration fails startup.
- [x] Same-camera session serialization and prepared-session revalidation remain green.
- [x] Backend, Home Assistant, Ruff, compile, JSON, Compose, Docker build, HACS, and Hassfest checks pass at the exact frozen SHA.
- [x] Added-line privacy scan finds no deployment-specific identifiers or secrets.

## Future deployment gate — NOT AUTHORIZED BY THIS DOCUMENT

Before any candidate deployment:

- [ ] Obtain explicit deployment approval.
- [ ] Verify the draft PR head and all required checks at the exact SHA.
- [ ] Back up `.env`, actual `/data`, Compose/source state, current image identity, and the matching Home Assistant integration.
- [ ] Build with exact `GIT_SHA` and verify image/OCI provenance.
- [ ] Configure only the representative G4 target and exact observed experimental profile.
- [ ] Keep the G3/Smart Chime production targets and state recoverable.

## Future physical gate — NO AUDIO WITHOUT A FRESH PROMPT

Test one physical device at a time. Immediately before each audible action, ask the repository owner whether they are in the correct room and ready to listen. Do not combine devices or send automatic retries.

1. **G4 normal speech:** one unique phrase, once; confirm clarity, correct target, and no other device output.
2. **G4 repeat:** one phrase with `repeat_times=2`; confirm exactly two sequential, non-overlapping repetitions.
3. **G4 serialization:** two rapid distinct requests; confirm correct sequential playback without overlap, stale audio, or duplicates.
4. **G4 recovery:** use a separately approved reversible outage method; verify unavailable/available state and then prompt again before one recovery phrase.
5. **Isolation:** verify camera-only playback leaves Smart Chime slot state unchanged.
6. **Restart:** separately approve Announcer restart, verify exact provenance/state, then prompt again before one post-restart G4 phrase.

A successful HTTP/WebSocket response is not proof of sound. Human observation is required. Any wrong, missing, duplicate, overlapping, or uncertain output stops the gate without automatic retry.

## Merge and publication boundary

Passing automated checks does not authorize deployment or sound. Passing physical validation does not authorize merge, tagging, publication, release deployment, stable promotion, or generic camera compatibility. Each remains a separate approval gate.
