# v2.2.0-beta.3 G4 Instant physical validation

## Current result

`RESULT: PASS — REPRESENTATIVE G4 INSTANT EXACT-PROFILE GATE`

All required physical gates passed on the exact runtime candidate. Normal, repeat-times-two, serialization, camera-reboot recovery, and post-Announcer-restart playback were owner-observed on the representative G4 Instant. The first post-restart observation was uncertain. No automatic retry was sent, and the deployment was rolled back fail-closed to beta.2. After explicit approval, the same immutable beta.3 candidate and matching HA integration were restored, Announcer alone was restarted, all silent checks passed, and one newly approved retry was heard clearly once at device speaker volume `100`. This result applies to the representative G4 Instant only. The later separately approved merge, tagging, publication, and release deployment do not promote beta.3 to stable or establish generic camera compatibility.

## Candidate identity

- Version: `2.2.0-beta.3`
- Candidate SHA: `4133c6e8804b30b59654b0324fe33a8af5d3dc82`
- Candidate image: `sha256:18982a417c5f0106e00b756988dd79ccaca029f3be747321b44a9f50d0d6aca2`
- Stable rollback target: `v2.1.8`
- Candidate-validation deployed prerelease rollback target at that time: `v2.2.0-beta.2`
- Representative target: one UVC G4 Instant

No private addresses, hostnames, credentials, device IDs, signed URLs, cookies, or private topology belong in this report.

## Read-only discovery — COMPLETE

- [x] Protect reports the representative G4 Instant connected and recording.
- [x] Protect reports a speaker.
- [x] Bootstrap metadata reports AAC, `serverudp`, 22,050 Hz, mono, 16-bit talkback.
- [x] The profile matches the existing low-level AAC/ADTS implementation dimensions.
- [x] No talkback session was negotiated or prepared.
- [x] No audio frame was sent.
- [x] No camera, Protect, Docker, or Home Assistant setting was changed.

This metadata match is necessary but not sufficient evidence of compatibility.

## Automated candidate gate

- [x] `EXPERIMENTAL_CAMERA_PROFILES` defaults to empty.
- [x] G4 remains unavailable without explicit opt-in.
- [x] Exact five-dimensional AAC profile opt-in reports `experimental_opt_in`.
- [x] Existing G3 profile remains `physically_validated`.
- [x] Profile mismatch fails before talkback preparation.
- [x] Malformed, duplicate, extra-key, unsupported, and type-coerced profile configuration fails startup.
- [x] Same-camera session serialization and prepared-session revalidation remain green.
- [x] Backend, Home Assistant, Ruff, compile, JSON, Compose, Docker build, HACS, and Hassfest checks pass at the exact frozen SHA.
- [x] Added-line privacy scan finds no deployment-specific identifiers or secrets.

## Deployment gate — COMPLETE

Before any candidate deployment:

- [x] Explicit deployment approval was obtained.
- [x] The draft PR head and all eight required checks passed at the exact SHA.
- [x] `.env`, actual `/data`, Compose/source state, current image identity, and the matching Home Assistant integration were backed up.
- [x] The backend was built with exact `GIT_SHA`; source, runtime SHA, image ID, and OCI revision agree.
- [x] Only the representative G4 target and exact observed experimental profile were added.
- [x] The G3 and Smart Chime remained available and recoverable.
- [x] Initial failed silent assertions automatically restored beta.2 before the corrected validation was rerun.

## Physical gate — PASS; NO AUDIO WITHOUT A FRESH PROMPT

Test one physical device at a time. Immediately before each audible action, ask the repository owner whether they are in the correct room and ready to listen. Do not combine devices or send automatic retries.

1. [x] **G4 normal speech:** “Beta three G4 normal camera test” was heard clearly once from the representative G4; no duplicate, overlap, or other-device output was reported.
2. [x] **G4 repeat:** “Beta three G4 repeat test” was heard exactly twice, sequentially and without overlap.
3. [x] **G4 serialization:** phrase A was heard once followed by phrase B once, without overlap, stale audio, duplicates, or other-device output.
4. [x] **G4 recovery:** the approved camera reboot produced unavailable → available recovery in 43.7 seconds; the camera returned connected and recording, and the post-recovery phrase was heard clearly once.
5. [x] **Isolation:** after every executed G4 playback, the G3 and Smart Chime queues returned to zero and Smart Chime slot ownership/content fingerprints were unchanged.
6. [x] **Restart:** the initial observation was uncertain, so no automatic retry was sent and beta.3 was rolled back. A later explicitly approved retry restored the same immutable candidate, repeated the Announcer-only restart, passed silent provenance/health/target/entity/queue/slot/log checks, and produced one clearly heard phrase at device speaker volume `100`.

## Fail-closed rollback and approved retry — COMPLETE

- [x] Backend restored to beta.2 source/runtime SHA `9d8e8f845201cf8de1223cc7d5fc31c192194a5d`.
- [x] Backend restored to immutable image `sha256:33c16a43051a643a6cd2b04cad5f90986d0a4c4153ea22534a1ca584318fb5f8` with matching OCI revision.
- [x] Home Assistant integration restored to `2.2.0-beta.2`; `ha core check` passed and config-entry reload required no restart.
- [x] The beta.3 G4 entity became unavailable while the validated G3 remained available.
- [x] The G4 camera itself was connected and recording after its approved reboot.
- [x] Smart Chime slot ownership/content proof remained unchanged and rollback logs contained no relevant errors.
- [x] The retry used the same runtime SHA and immutable image; no code candidate changed between the uncertain observation and the passing retry.
- [x] Camera API volume override remained omitted because camera talkback rejects overrides; Protect confirmed the actual device speaker volume was `100` immediately before playback.
- [x] The retry was one phrase, once, on one device, after a fresh room-readiness confirmation.

A successful HTTP/WebSocket response is not proof of sound. Human observation is required. Any wrong, missing, duplicate, overlapping, or uncertain output stops the gate without automatic retry.

## Tagged release deployment

`TAGGED RELEASE DEPLOYMENT: PASS`

- [x] PR #40 was squash-merged to `main` at `76fca7f25a8f3b43e73ffd0432d97e5ec3995ce4`; all trusted post-merge checks passed.
- [x] Tag and GitHub prerelease `v2.2.0-beta.3` point to that exact SHA.
- [x] Direct tree comparison from the physically tested candidate found only documentation and public-install contract-test changes; executable/configuration diff count was zero.
- [x] Tagged source, runtime SHA, OCI revision, and pinned image `sha256:46b1af0a5a96a9b93d0c08884b7f07a6a669a59ac170cc713ee2173837dc1966` agree.
- [x] The installed Home Assistant integration's 20 release files match the tag byte-for-byte and its version sensor reports the tagged SHA.
- [x] Silent smoke checks passed with health OK, zero restarts/errors, available G3/G4 entities, zero queues, and unchanged/ready Smart Chime slots with no legacy orphans.
- [x] No additional audio was played because the approved tagged deployment contained no executable/configuration change from the physically tested candidate.
- [x] Verified rollback artifacts remain retained.

Publication and deployment do not promote beta.3 to stable or broaden evidence beyond the representative G4 Instant exact profile.
