# v2.2.0-beta.2 camera hardening physical validation

## Result

`RESULT: PASS`

The repository owner explicitly waived Gate 6 (unavailable mixed-group fail-closed) after reviewing the reversible offline procedure. Gate 6 was **not empirically executed** and this PASS does not claim otherwise. All other required physical gates were completed on the exact candidate.

## Exact candidate

- Git/source revision: `b1d0bcf5aaaaf4ffffcc98fb274ff325f3923a50`
- Backend semantic version: `2.2.0-beta.2`
- Home Assistant integration version: `2.2.0-beta.2`
- Docker image ID: `sha256:f29bda4b3251f7e488baed626cc096948e4d4beccd51147c92a4b42c22dce9e1`
- OCI revision label: `b1d0bcf5aaaaf4ffffcc98fb274ff325f3923a50` (exact match)
- Protect version reported by the candidate: `7.2.105`
- Tested camera model: `UVC G3 Instant`
- Stable rollback target: `v2.1.8`

No private addresses, hostnames, credentials, cookies, signed URLs, Protect IDs, or private topology are recorded here.

## Preflight

- [x] PR head and authoritative remote branch matched the exact candidate SHA before deployment.
- [x] Required GitHub checks were green at the exact candidate: backend pytest, Ruff, compile/JSON validation, Docker Compose validation, Docker build, Home Assistant tests, HACS, and Hassfest.
- [x] Production environment, actual persistent data, Compose/source state, container state, and current HA integration were backed up with restrictive permissions.
- [x] Backup archives listed successfully and checksums verified.
- [x] Exact candidate image was built with the candidate `GIT_SHA`.
- [x] OCI revision, `/version`, backend version, and candidate SHA agreed.
- [x] `/health` was healthy.
- [x] Exactly two Smart Chime dynamic slots were ready before camera testing.
- [x] Matching beta.2 HA integration loaded successfully.
- [x] Existing HA entities remained present; no duplicate unique IDs were created.

## G3 Instant compatibility gate

Authenticated `/targets` reported:

- [x] target type `camera`;
- [x] model `UVC G3 Instant`;
- [x] `status=available`;
- [x] `compatibility=physically_validated`;
- [x] `announce=true` and `repeat=true`;
- [x] volume/preset/default/buzzer capabilities false.

## Physical playback gates

### 1. Normal speech — PASS

- [x] A unique announcement was sent through Home Assistant.
- [x] The intended G3 Instant spoke clearly once.
- [x] No duplicate or Smart Chime speech occurred.
- [x] HA playback result became success and the camera queue returned to zero.
- [x] Smart Chime slot fingerprint remained unchanged.

### 2. `repeat_times=2` — PASS

- [x] One request used `repeat_times=2`.
- [x] Exactly two sequential repetitions were heard.
- [x] No overlap, unexpected third replay, or retry occurred.

### 3. Same-camera concurrency/session serialization — PASS

- [x] Two distinct same-priority requests were sent concurrently to the same G3 Instant.
- [x] Both phrases played correctly and sequentially, without overlap, stale audio, duplicates, or retries.
- [x] Logs showed talkback session preparation separated in time rather than simultaneously armed.
- [x] The camera queue returned to zero.
- [x] A second concurrent pair with different priorities also played correctly and sequentially.

### 4. Dynamic offline → online recovery — PASS

- [x] Camera power was removed and restored using a reversible, non-destructive method.
- [x] Without restarting Announcer, `/targets` changed to `status=unavailable` / `announce=false`.
- [x] Existing HA camera entities remained registered and became unavailable without integration reload.
- [x] After restoration, `/targets` returned to `status=available` / `announce=true`.
- [x] The same HA entity became available again without reload/restart.
- [x] A recovery announcement played clearly once from the G3 Instant with no Smart Chime speech.

### 5. Valid mixed Smart Chime + camera group — PASS

- [x] A temporary valid group contained one Smart Chime and the validated G3 Instant.
- [x] The group exposed the expected intersected capabilities.
- [x] A unique mixed announcement returned success.
- [x] The observer heard the correct phrase from both the G3 Instant and Smart Chime.
- [x] No stale or duplicate audio occurred.

An initial mixed-group attempt was inaudible at the Smart Chime because the deployment's global default was set to volume 1 and mixed camera groups intentionally omit/reject per-request volume overrides. A same-content Chime-only replay at volume 50 was clear and was a fixed-slot content hit with zero overwrite. The exact candidate was then redeployed with a temporary global default of 50 and the mixed-group gate passed on the first corrected attempt. This was a validation-configuration issue, not a candidate source change.

### 6. Unavailable mixed-group fail-closed — OWNER WAIVER, NOT EMPIRICALLY RUN

- [ ] The offline mixed-group request was not executed.
- [x] The repository owner explicitly approved skipping this physical gate and treating the waiver as accepted for this candidate.
- [x] No claim of empirical fail-closed playback evidence is made.
- [x] Automated tests covering unavailable-camera preflight and prevention of Chime side effects remained green at the exact candidate SHA.

### 7. Camera-only Smart Chime isolation — PASS

- [x] Smart Chime slot state was captured before and after a camera-only request.
- [x] The G3 Instant spoke the correct phrase once.
- [x] No Smart Chime speech occurred.
- [x] The before/after slot-status JSON and SHA-256 fingerprint were exactly unchanged.
- [x] The response contained no dynamic Smart Chime slot allocation.

### 8. Announcer restart — PASS

- [x] Only the Announcer service was restarted using the same candidate image.
- [x] Backend version, Git SHA, image ID, and OCI revision still matched the candidate.
- [x] `/targets` revalidated the G3 Instant as available and announcement-capable.
- [x] The post-restart camera phrase played clearly once with no Smart Chime speech.
- [x] Dynamic-slot assignments and identity structure remained intact; only process/runtime status metadata changed across restart.

## Restoration

- [x] The temporary mixed group and temporary volume-50 validation override were removed.
- [x] The prior backend and HA integration deployment was restored after testing.
- [x] Restored backend and HA service health were verified.
- [x] No merge, tag, GitHub release, image publication, or stable promotion was performed as part of validation.

## Final disposition

The exact `v2.2.0-beta.2` runtime candidate passed Gates 1–5 and 7–8. Gate 6 was explicitly waived by the repository owner and remains clearly identified as untested. Evidence is documentation-only; any subsequent runtime-code change requires a new candidate SHA and full revalidation.

## Tagged release and deployment smoke test

- [x] Immutable prerelease `v2.2.0-beta.2` was published at merge SHA `9d8e8f845201cf8de1223cc7d5fc31c192194a5d`.
- [x] Both tag-triggered workflow suites passed for that exact SHA.
- [x] The tagged Docker backend reported `2.2.0-beta.2` and the exact release SHA through `/version` and its OCI revision label.
- [x] The production override pinned the running image digest `sha256:33c16a43051a643a6cd2b04cad5f90986d0a4c4153ea22534a1ca584318fb5f8`.
- [x] The matching Home Assistant integration reported `2.2.0-beta.2`; config-entry reload returned `require_restart: false`.
- [x] Backend, Protect, event-stream, direct-device, Chime, and two-slot TTS health checks passed with zero container restarts.
- [x] After explicit room-readiness confirmation, one G3 Instant announcement played clearly once with no other target speaking.
- [x] After a separate readiness confirmation, one Smart Chime announcement played clearly once at volume 50.
- [x] No current error lines were found in the narrow post-test log window.

The tagged smoke test did not rerun the waived Gate 6 and does not change its evidence status.
